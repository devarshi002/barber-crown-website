name: Keep Render Server Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # Har 10 minute mein run hoga
  workflow_dispatch:          # Manual trigger bhi kar sakte ho

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Server
        run: |
          echo "🏓 Pinging Render server..."
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://barber-crown-apii.onrender.com/api/health)
          echo "📡 Response status: $STATUS"
          if [ "$STATUS" = "200" ]; then
            echo "✅ Server is alive!"
          else
            echo "⚠️ Server returned status: $STATUS"
          fi